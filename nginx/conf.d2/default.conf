# If we receive X-Forwarded-Proto, pass it through; otherwise, pass along the
# scheme used to connect to this server
map $http_x_forwarded_proto $proxy_x_forwarded_proto {
  default $http_x_forwarded_proto;
  ''      $scheme;
}
# If we receive X-Forwarded-Port, pass it through; otherwise, pass along the
# server port the client connected to
map $http_x_forwarded_port $proxy_x_forwarded_port {
  default $http_x_forwarded_port;
  ''      $server_port;
}
# If we receive Upgrade, set Connection to "upgrade"; otherwise, delete any
# Connection header that may have been passed to this server
map $http_upgrade $proxy_connection {
  default upgrade;
  '' close;
}
# Apply fix for very long server names
server_names_hash_bucket_size 128;
# Default dhparam
ssl_dhparam /etc/nginx/dhparam/dhparam.pem;
# Set appropriate X-Forwarded-Ssl header
map $scheme $proxy_x_forwarded_ssl {
  default off;
  https on;
}
gzip_types text/plain text/css application/javascript application/json application/x-javascript text/xml application/xml application/xml+rss text/javascript;
log_format vhost '$host $remote_addr - $remote_user [$time_local] '
                 '"$request" $status $body_bytes_sent '
                 '"$http_referer" "$http_user_agent"';
access_log off;
resolver 127.0.0.11;
# HTTP 1.1 support
proxy_http_version 1.1;
proxy_buffering off;
proxy_set_header Host $http_host;
proxy_set_header Upgrade $http_upgrade;
proxy_set_header Connection $proxy_connection;
proxy_set_header X-Real-IP $remote_addr;
proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
proxy_set_header X-Forwarded-Proto $proxy_x_forwarded_proto;
proxy_set_header X-Forwarded-Ssl $proxy_x_forwarded_ssl;
proxy_set_header X-Forwarded-Port $proxy_x_forwarded_port;
# Mitigate httpoxy attack (see README for details)
proxy_set_header Proxy "";
server {
	server_name _; # This is just an invalid value which will never trigger on a real hostname.
	listen 80;
	access_log /var/log/nginx/access.log vhost;
	return 503;
}
# alyson.*
upstream alyson.* {
				## Can be connected with "gennymain_mainproxy" network
			# genny-main_alyson_1
			server 172.18.0.10:80;
}
server {
	server_name alyson.*;
	listen 80 ;
	access_log /var/log/nginx/access.log vhost;
	include /etc/nginx/vhost.d/default;
	location / {
		proxy_pass http://alyson.*;
	}
}
# api.*
upstream api.* {
				## Can be connected with "gennymain_mainproxy" network
			# qwanda-service
			server 172.18.0.11:8080;
}
server {
	server_name api.*;
	listen 80 ;
	access_log /var/log/nginx/access.log vhost;
	include /etc/nginx/vhost.d/default;
	location / {
		proxy_pass http://api.*;
	}
}
# bouncer.*
upstream bouncer.* {
				## Can be connected with "gennymain_mainproxy" network
			# keycloak
			server 172.18.0.6:8080;
}
server {
	server_name bouncer.*;
	listen 80 ;
	access_log /var/log/nginx/access.log vhost;
	include /etc/nginx/vhost.d/default;
	location / {
		proxy_pass http://bouncer.*;
	}
}
# keycloak.*
upstream keycloak.* {
				## Can be connected with "gennymain_mainproxy" network
			# keycloak
			server 172.18.0.6:8080;
}
server {
	server_name keycloak.*;
	listen 80 ;
	access_log /var/log/nginx/access.log vhost;
	include /etc/nginx/vhost.d/default;
	location / {
		proxy_pass http://keycloak.*;
	}
}
# local.*
upstream local.* {
				## Can be connected with "gennymain_mainproxy" network
			# genny-main_alyson_1
			server 172.18.0.10:80;
}
server {
	server_name local.*;
	listen 80 ;
	access_log /var/log/nginx/access.log vhost;
	include /etc/nginx/vhost.d/default;
	location / {
		proxy_pass http://local.*;
	}
}
# payments.genny.life
upstream payments.genny.life {
				## Can be connected with "gennymain_mainproxy" network
			# payments
			server 172.18.0.5:3456;
}
server {
	server_name payments.genny.life;
	listen 80 ;
	access_log /var/log/nginx/access.log vhost;
	include /etc/nginx/vhost.d/default;
	location / {
		proxy_pass http://payments.genny.life;
	}
}
# qwanda-service.*
upstream qwanda-service.* {
				## Can be connected with "gennymain_mainproxy" network
			# qwanda-service
			server 172.18.0.11:8080;
}
server {
	server_name qwanda-service.*;
	listen 80 ;
	access_log /var/log/nginx/access.log vhost;
	include /etc/nginx/vhost.d/default;
	location / {
		proxy_pass http://qwanda-service.*;
	}
}
# uppy.genny.life
upstream uppy.genny.life {
				## Can be connected with "gennymain_mainproxy" network
			# uppy
			server 172.18.0.4:3020;
}
server {
	server_name uppy.genny.life;
	listen 80 ;
	access_log /var/log/nginx/access.log vhost;
	include /etc/nginx/vhost.d/default;
	location / {
		proxy_pass http://uppy.genny.life;
	}
}
# v2.*
upstream v2.* {
				## Can be connected with "gennymain_mainproxy" network
			# genny-main_alyson_1
			server 172.18.0.10:80;
}
server {
	server_name v2.*;
	listen 80 ;
	access_log /var/log/nginx/access.log vhost;
	include /etc/nginx/vhost.d/default;
	location / {
		proxy_pass http://v2.*;
	}
}
