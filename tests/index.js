import { Login } from './Login';
import { Logout } from './Logout';
import { Registration } from './Registration';

/*Login() 
.then(() => {

return Logout();
})
.then(() => {
  return Registration();
})
.catch(e => {
  console.error (e );
})

export default this;
export.modules.id = "Authentification";*/